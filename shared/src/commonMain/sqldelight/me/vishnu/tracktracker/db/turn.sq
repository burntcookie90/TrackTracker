CREATE TABLE turn (
    id TEXT PRIMARY KEY NOT NULL,
    number INTEGER NOT NULL,
    note_id INTEGER,
    track_id TEXT NOT NULL
);

addTurnNote:
INSERT INTO turn(id, number, note_id, track_id)
VALUES (?, ?, ?, ?);